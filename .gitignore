*.fasl
*.abcl
*.lx64fsl
*~
.#*
/docs/*.tmp

# Local working rules — names private infrastructure, must not be published.
/CLAUDE.md

# Render output from `make smoke` / the test suites.
/out/

# Release artefacts: `make binary` and packaging/build-appimage.sh.
/dist/
